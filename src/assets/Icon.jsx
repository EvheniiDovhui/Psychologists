import icon from './img/sprite.svg'

export const IconUser = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconUser`}></use>
	</svg>
)

export const IconUsers = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconUsers`}></use>
	</svg>
)

export const IconCheck = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconCheck`}></use>
	</svg>
)

export const IconVector23 = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconVector23`}></use>
	</svg>
)

export const IconClock = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconClock`}></use>
	</svg>
)

export const IconArrow = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconArrow`}></use>
	</svg>
)

export const IconNoFavorites = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconNoFavorites`}></use>
	</svg>
)

export const IconEye = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconEye`}></use>
	</svg>
)

export const IconEyeOff = ({ className }) => (
	<svg className={`${className}`}>
		<use href={`${icon}#iconEyeOff`}></use>
	</svg>
)
